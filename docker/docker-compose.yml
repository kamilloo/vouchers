web:
  image: nginx:latest
  ports:
    - "8080:80"
  volumes:
    - ../:/var/www
    - ./nginx.conf:/etc/nginx/conf.d/default.conf
  links:
    - php
#    - mysql

php:
  build: .
  volumes:
    - ../:/var/www

#mysql:
#  image: mysql:5.7
#  restart: always
#  environment:
#    MYSQL_DATABASE: 'vouchers'
#    MYSQL_USER: 'root'
#    MYSQL_PASSWORD: 'root'
#    MYSQL_ROOT_PASSWORD: 'root'
#  ports:
#    - '3306:3306'
#  expose:
#    - '3306'
#  volumes:
#    - my-db:/var/lib/mysql
