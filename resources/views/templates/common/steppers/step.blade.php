<div class="step" data-target="#{{ $target }}">
    <button type="button" class="step-trigger" role="tab" aria-controls="{{ $target }}" id="{{ $target }}-trigger">
        <span class="bs-stepper-circle">{{ $counter }}</span>
        <span style="font-size: .5rem" class="bs-stepper-label">{{ $label }}</span>
    </button>
</div>
